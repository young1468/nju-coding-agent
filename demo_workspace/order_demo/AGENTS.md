# Order Demo Rules

- Do not modify files under `tests/`.
- Use only the Python standard library; do not add dependencies.
- Preserve the public `checkout(catalog, items, coupon=None)` and CLI item/coupon interfaces.
- Run `python -m pytest -q` from this directory before reporting completion.
